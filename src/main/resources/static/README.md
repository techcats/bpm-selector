# BPM Selector Static

Static resources for BPM Selector service

## Setup

```
npm install -g bower
npm install -g gulp
npm install
bower install
```

See `gulpfile.js` for task config.
